import { ComingSoon } from "@/components/ComingSoon";

export default function RewardsPage() {
  return <ComingSoon title="Rewards" body="A dedicated view for managing reward inventory across quests is still being built. For now, set a quest's reward when you create it." />;
}
