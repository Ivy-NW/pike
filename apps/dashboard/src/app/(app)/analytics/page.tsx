import { ComingSoon } from "@/components/ComingSoon";

export default function AnalyticsPage() {
  return <ComingSoon title="Analytics" body="Cross-venue trends and historical reporting aren't built yet. Per-quest redemption stats are already available from each quest's detail page." />;
}
