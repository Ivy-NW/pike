NEXT_PUBLIC_API_BASE_URL="http://localhost:4000"
NEXT_PUBLIC_DASHBOARD_URL="http://localhost:3001"

# Server-only (no NEXT_PUBLIC_ prefix) -- read only by src/app/api/admin-gate/redirect/route.ts.
# Never expose this as NEXT_PUBLIC_*: that would ship the admin hostname in the public JS bundle.
ADMIN_URL="http://localhost:3002"
